{
  "name": "fushenglu",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "浮生錄：iPhone TauriTavern / SillyTavern 純前端擴充",
  "scripts": {
    "check": "node --check src/index.js && node --check src/core/chat-state.js && node --check src/core/analysis-schema.js && node --check src/core/api-client.js && node --check src/core/turn-sync.js && node --check src/integrations/tauritavern.js && node --check src/ui/app.js && node --check scripts/build.mjs && node --check build.config.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "verify": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  }
}
