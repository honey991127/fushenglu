# 原始碼

- `index.js`：第三方 UI extension 入口。
- `style.css`：手機優先入口與全屏頁樣式。
- `core/chat-state.js`：最小 `ChatState`、驗證與遷移。
- `integrations/tauritavern.js`：公開宿主接口偵測與逐聊天 metadata 儲存。
- `ui/app.js`：插件自己的入口、全屏頁與儲存測試控制。

此目錄沒有從 `mockups/ui-v13.html` 複製正式功能，也不包含任何後續遊戲模組。
